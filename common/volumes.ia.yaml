volumes:

  flualfadata:
    name: flualfadata
    driver: local
    external: true
    driver_opts:
      type: none
      device: /volumes/flualfadata/_data
      o: bind

  certificates:
    name: certificates
    driver: local
    external: true

  jupyterdata:
    name: jupyterdata
    driver: local
    external: true
    driver_opts:
      type: none
      device: /volumes/jupyterdata/_data
      o: bind

  jupytershared:
    name: jupytershared
    driver: local
    external: true
    driver_opts:
      type: none
      device: /volumes/jupyterdata/_data
      o: bind
