networks:
  netlab01:
    driver: bridge
    name: netlab01
    ipam:
      config:
        - subnet: '172.1.0.0/16'

volumes:
  certificates:
    name: certificates
    driver: local
  sites:
    name: sites
    driver: local